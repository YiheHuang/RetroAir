# 项目计划书：城市环境质量多模态评估与预测系统

> **课程**：数据分析与挖掘 · 期末项目
> **选题类型**：C（端到端应用开发，融合A多源数据 + B算法对比）
> **团队规模**：4人
> **项目周期**：5周（第13-17周）

---

## 一、项目题目与目的

### 1.1 题目

**基于多模态数据融合的城市空气质量精细化预测系统**

### 1.2 项目目的

中国168个主要城市设有官方空气质量监测站，但每个城市仅部署3-15个站点，覆盖密度极低。**监测站之外的地点——住宅小区、学校、办公区——没有空气质量数据。** 而空气质量并非随机分布，它与周边土地利用（工业区/绿地/水体）、交通路网密度、建筑密度、气象条件高度相关。

本项目旨在回答一个核心问题：**能否利用多源公开数据（卫星遥感图像 + 城市基础设施 + 气象条件），对任意地点的PM2.5浓度进行可靠推断？**

### 1.3 选题定位

本项目以选项C（端到端应用开发）为主线，天然内嵌选项A（三源异构数据融合）和选项B（多算法系统对比）的技术深度：

- **多源数据融合（A）**：卫星图像（Sentinel-2）+ 城市基础设施（OpenStreetMap）+ 气象条件（ERA5），三种模态物理意义不同、数据结构不同、时空粒度不同，融合具有真实技术挑战
- **算法对比（B）**：虽然本项目以最优融合模型为核心交付物，但实验设计中必然包含单模态基线、传统ML基线、不同融合策略的消融对比，构成系统性算法评估
- **端到端应用（C）**：交付Streamlit交互式Dashboard，用户可浏览AQI监测地图、查看模型预测、分析各模态贡献度

### 1.4 输入与输出

**输入（三模态异构数据）：**

| 模态 | 数据源 | 内容 | 数据结构 | 维度 |
|------|--------|------|---------|------|
| 模态1：卫星遥感 | Sentinel-2 L2A | 监测站周边1km×1km地表反射率影像（RGB+NIR） | 图像（224×224×3） | 经ResNet编码后2048维 |
| 模态2：城市场景 | OpenStreetMap | 缓冲区（1km/3km/5km）内POI密度、路网长度、建筑/绿地/水体占比 | 结构化表格 | ~30维 |
| 模态3：气象条件 | Open-Meteo | 日均温度、相对湿度、风速、风向、气压、降水量、云量 | 结构化表格 | 7维 |

**输出：**

| 输出类型 | 内容 | 任务类型 |
|---------|------|---------|
| 主输出 | PM2.5浓度值（μg/m³） | 回归 |
| 辅助输出 | AQI六等级分类（优/良/轻度/中度/重度/严重污染） | 分类 |
| 可解释性输出 | 各模态注意力权重、SHAP特征重要性排名 | 分析 |

---

## 二、数据收集方式

### 2.1 数据源总览

全部数据源自公开免费渠道，无需申请权限，Python API一键获取。

```
数据源                          获取方式                    覆盖范围
──────────────────────────────────────────────────────────────────
AKShare (中国环境监测总站)  →  akshare Python库           168城市, 2014至今
Sentinel-2 L2A              →  Copernicus Data Space API   全球, 10m分辨率
OpenStreetMap               →  osmnx Python库             全球
Open-Meteo                  →  openmeteo Python库          全球, 无需API Key
EuroSAT (预训练基准)         →  HuggingFace datasets       27K标注样本
```

### 2.2 各模态详细收集方案

#### 模态1：卫星遥感影像

| 项目 | 说明 |
|------|------|
| **数据产品** | Sentinel-2 L2A（ESA大气校正产品，地表反射率） |
| **波段选择** | RGB三波段（B04-Red, B03-Green, B02-Blue），空间分辨率10m |
| **时空范围** | 10-15个目标城市 × 每站4季各1景（春/夏/秋/冬快照） |
| **提取方法** | 以每个AQI监测站坐标为中心，裁剪1km×1km正方形区域（~100×100像素），resize至224×224 |
| **云处理** | 优先选择云覆盖率<10%的影像；若某季无有效影像，使用ESA WorldCover土地覆盖产品作为替代特征 |
| **下载工具** | `sentinelsat` Python库 或 Google Earth Engine Python API（`ee`） |
| **存储格式** | PNG/GeoTIFF文件，按 `{城市}/{站点ID}/{季节}.png` 组织 |

**备选方案（降低难度）：**
若Sentinel-2原始影像下载因网络/存储原因受阻，可降级为使用ESA WorldCover（10m全球土地覆盖产品，单张GeoTIFF即可获取11类土地覆盖比例），或使用Dynamic World（准实时10m土地覆盖）。此方案失去深度学习图像处理环节，但保证项目可完成。

#### 模态2：城市基础设施（OpenStreetMap）

| 项目 | 说明 |
|------|------|
| **提取工具** | `osmnx` Python库（`osmnx.features_from_point()`） |
| **缓冲区层级** | 以监测站为中心，1km / 3km / 5km 三个半径的圆形缓冲区 |
| **提取时间** | 取最新OSM快照（OSM数据持续更新，对城市基础设施而言变化缓慢，单次提取即可） |

**每缓冲区提取的特征（共约30维）：**

```
特征类别             具体特征                           特征数
──────────────────────────────────────────────────────────
工业与排放源        工厂数、加油站数、垃圾处理站数           3
交通               高速公路长度(m)、主干道长度(m)、          5
                   次干道长度(m)、交叉口数、公交站数
商业与生活          餐饮POI数、商场数、写字楼数              3
绿地与水体          公园面积(m²)、森林面积(m²)、             3
                   水体面积(m²)
土地利用占比        建筑占地比、绿地占比、水体占比            3
人口活动            住宅楼栋数、学校数、医院数                3
综合指标            路网总密度(km/km²)、POI总密度(个/km²)    2
                                  每缓冲区合计            22
                        × 3缓冲区 ≈ 66维 → PCA/筛选至 ~30维
```

#### 模态3：气象条件

| 项目 | 说明 |
|------|------|
| **数据源** | Open-Meteo Archive API（基于ERA5再分析数据） |
| **提取工具** | `openmeteo_requests` Python库 |
| **时间范围** | 2023-01-01 至 2024-12-31，每日 |
| **空间匹配** | 以每个监测站坐标查询最近网格点（ERA5网格~0.25°≈28km，对城市尺度气象足够） |

**每日气象字段（7维）：**

| 字段 | 简写 | 单位 | 说明 |
|------|------|------|------|
| 2m温度 | t2m | °C | 日均温 |
| 相对湿度 | rh | % | 日均湿度 |
| 10m风速 | ws | m/s | 日均风速 |
| 10m风向 | wd | ° | 日均风向（sin/cos编码为2维） |
| 地表气压 | sp | hPa | 日均气压 |
| 总降水量 | tp | mm | 日累计降水 |
| 总云量 | tcc | % | 日均云覆盖 |

#### 标签数据：AQI/PM2.5

| 项目 | 说明 |
|------|------|
| **数据源** | 中国环境监测总站，通过 `akshare.air_quality_rank()` 获取 |
| **获取方式** | 逐日调用API，168城每日AQI数据 |
| **目标字段** | `pm2.5`（主目标，回归）、`quality`（质量等级，分类标签） |
| **时间范围** | 2023-01-01 至 2024-12-31（2年，~730天/站） |
| **存储格式** | CSV → `data/raw/aqi_daily.csv` |

### 2.3 城市选择策略

选择标准：
1. OSM数据覆盖完善（一线/新一线城市优先）
2. 地理多样性（沿海/内陆/北方/南方各取代表）
3. 空气质量变异性（避免全年"优"或全年"污染"的单调节奏）

建议初选城市（15个）：
> 北京、上海、广州、成都、武汉、西安、沈阳、南京、杭州、郑州、重庆、长沙、石家庄、兰州、昆明

### 2.4 数据管线总览

```
Week 1-2: 数据获取阶段
─────────────────────────────────────────────────────────────────
AKShare ──→ 逐日爬取AQI ──→ data/raw/aqi_2023.csv
                                data/raw/aqi_2024.csv

OSM      ──→ 缓冲区提取  ──→ data/raw/osm_features.csv

Open-Meteo ─→ 逐日气象   ──→ data/raw/weather_2023.csv
                                data/raw/weather_2024.csv

Sentinel-2 ─→ 四季影像    ──→ data/satellite/{station}/{season}.png

Week 2: 数据对齐阶段
─────────────────────────────────────────────────────────────────
所有数据按 (station_id, date) 对齐
    → data/processed/merged_dataset.h5
    约 730天 × 100站 = 73,000 条样本（去除缺失后约 50,000 条）
```

---

## 三、核心算法设计

### 3.1 算法选择：多模态交叉注意力融合网络（MM-CAF）

不采用随机森林、XGBoost或简单MLP，而选择**多模态交叉注意力融合网络（Multi-Modal Cross-Attention Fusion Network, MM-CAF）**，原因如下：

| 评估维度 | RF/XGBoost | 简单MLP拼接 | MM-CAF（本项目选择） |
|---------|-----------|------------|-------------------|
| 处理异构模态 | 需手工特征工程 | 直接拼接，无视模态差异 | 独立编码器→交叉注意力 |
| 模态间交互 | 弱（特征叉乘） | 弱（MLP隐式学习） | **强（显式Attention）** |
| 可解释性 | SHAP（事后） | 差 | **注意力权重矩阵**天然可解释 |
| 单GPU训练 | ✓ | ✓ | ✓（batch_size=64） |
| 答辩表现力 | 一般 | 一般 | **强（注意力可视化 + 模态贡献度）** |

### 3.2 网络架构

```
                          输入层
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌────▼────┐       ┌─────▼─────┐
   │卫星图像  │       │OSM特征  │       │ 气象特征   │
   │224×224×3│       │ [30-d]  │       │  [7-d]    │
   └────┬────┘       └────┬────┘       └─────┬─────┘
        │                  │                  │
   ┌────▼────┐       ┌────▼────┐       ┌─────▼─────┐
   │ResNet-50│       │ 2层MLP  │       │ 1层MLP    │
   │(冻结层1-3│       │128→256  │       │7→64→128   │
   │ 微调层4) │       │+ReLU+BN │       │+ReLU+BN  │
   └────┬────┘       └────┬────┘       └─────┬─────┘
        │ [2048-d]        │ [256-d]          │ [128-d]
        │                  │                  │
   ┌────▼────┐       ┌────▼────┐       ┌─────▼─────┐
   │投影层    │       │投影层    │       │ 投影层     │
   │2048→256 │       │256→256  │       │ 128→256   │
   └────┬────┘       └────┬────┘       └─────┬─────┘
        │ [256-d]         │ [256-d]          │ [256-d]
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
              ┌────────────▼────────────┐
              │   交叉注意力融合层       │
              │                        │
              │  sat_feat(Q) × osm_feat(K,V) → enhanced_sat
              │  osm_feat(Q) × met_feat(K,V) → enhanced_osm
              │  met_feat(Q) × sat_feat(K,V) → enhanced_met
              │                        │
              │  每路: MultiHeadAttn(d=256, h=8, dropout=0.1)
              │  + Residual + LayerNorm  │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │    融合特征拼接          │
              │ [enhanced_sat | enhanced_osm | enhanced_met]
              │               [768-d]    │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │    预测头 (MLP)          │
              │  768 → 512 → 256 → 128  │
              │  + ReLU + Dropout(0.3)  │
              │  + BatchNorm            │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │        输出              │
              │  回归头: 128 → 1 (PM2.5) │
              │  分类头: 128 → 6 (AQI等级)│
              └──────────────────────────┘
```

### 3.3 关键设计决策

**1. 为什么ResNet-50只微调层4？**
- 层1-3学到的是通用视觉特征（边缘/纹理/形状），在ImageNet/EuroSAT上已充分训练
- 层4学到的是任务相关的高层语义（对EuroSAT而言是土地覆盖类型），仅微调此层：降低过拟合风险 + 加速训练（可训练参数减少70%）
- EuroSAT预训练权重来自HuggingFace `blanchon/EuroSAT_RGB`，比ImageNet预训练更适配遥感场景

**2. 为什么交叉注意力是3对而不是6对？**
- 3个模态全排列是6对交互（A→B, A→C, B→A, B→C, C→A, C→B），但双向注意力通常冗余
- 选取3对有向交互覆盖全模态（卫星→OSM, OSM→气象, 气象→卫星），信息在后续MLP中可进一步混合
- 减少参数量，降低小样本过拟合风险

**3. 为什么所有模态都投影到256维？**
- 统一维度简化交叉注意力计算
- 256是Multi-Head Attention (h=8) 的友好值（256/8=32维/头）
- 在所有模态的"信息瓶颈"和"表达力"之间取平衡

### 3.4 损失函数

```
回归损失: Smooth L1 Loss（比MSE对异常AQI值鲁棒）
分类损失: Cross Entropy Loss（6类AQI等级）
总损失  : L_total = λ_reg × L_smooth_l1 + λ_cls × L_ce
         其中 λ_reg=0.7, λ_cls=0.3（回归为主，分类辅助正则化）
```

### 3.5 训练配置

| 超参数 | 值 | 说明 |
|--------|-----|------|
| Optimizer | AdamW | 含权重衰减的Adam |
| Learning Rate | 1e-4（初始） | Cosine Annealing调度 |
| Weight Decay | 1e-4 | L2正则化 |
| Batch Size | 64 | 适配6GB显存 |
| Epochs | 100（Early Stop=15） | 监控验证集R² |
| Dropout | 0.1（注意力）, 0.3（MLP头） | 防止过拟合 |
| 数据划分 | 70%/15%/15%（按时间切分） | 训练/验证/测试，杜绝时间泄露 |

### 3.6 消融实验（计划内验证）

虽以MM-CAF为唯一交付模型，但训练过程中必然运行以下消融以验证融合有效性：

| 实验编号 | 输入 | 模型 | 目的 |
|---------|------|------|------|
| E0 | 仅卫星图 | ResNet-50 → MLP | 单模态下界 |
| E1 | 仅OSM | XGBoost | 结构化特征下界 |
| E2 | 仅气象 | XGBoost | 时序特征下界 |
| E3 | 卫星+OSM | 拼接→MLP | 双模态基准 |
| E4 | 卫星+OSM+气象 | 拼接→MLP（无注意力） | 三模态-无注意力 |
| **E5** | **卫星+OSM+气象** | **MM-CAF（本项目）** | **完整融合模型** |

E0-E4作为实验章节的数据来源，但**不作为独立交付物**，它们的代码内嵌于训练脚本中，通过配置文件切换。

---

## 四、团队分工与落实方式

### 4.1 角色分配

| 角色 | 职责 | 核心技术栈 | 可量化产出 |
|------|------|-----------|-----------|
| **数据工程负责人** | 四源数据获取、清洗、对齐；特征工程Pipeline；数据版本管理 | `akshare`, `osmnx`, `openmeteo_requests`, `sentinelsat`, `pandas`, `h5py` | `data_pipeline.py`, `feature_extraction.py`, `data/processed/merged_dataset.h5`, 数据字典文档 |
| **算法负责人** | MM-CAF网络实现、训练/验证循环、超参数调优、模型保存/导出 | `PyTorch`, `torchvision`, `timm`, `einops` | `model/mm_caf.py`, `train.py`, `config.yaml`, 训练日志, checkpoint文件 |
| **评估分析负责人** | 评估指标体系、消融实验执行、SHAP/注意力可视化、错误分析 | `sklearn`, `shap`, `plotly`, `matplotlib` | `evaluation.py`, `visualization/`, 消融实验表, 分析报告章节 |
| **工程交付负责人** | Streamlit应用（4模块）、一键运行脚本、Git管理、README、PPT、视频 | `Streamlit`, `Folium`, `Plotly`, `Git` | `app.py`, `run_all.sh`, 项目文档, 答辩材料 |

### 4.2 协作方式

- **代码仓库**：GitHub/Gitee，`main` + `dev` 分支 + 每人feature分支
- **每日同步**：5分钟站会（线上），同步进度和阻塞
- **Code Review**：Merge Request机制，至少一人Review后合并
- **实验记录**：共享在线表格，记录每次实验的超参数-指标-备注
- **文档协作**：Notion/飞书文档，报告分章节协作

### 4.3 落实方式与里程碑

```
Week 1 ───┬─ 第13周 ─── 项目计划书提交 ──────────────── 5%成绩节点
           │
           ├─ 数据源验证脚本跑通（verify_data_pipeline.py）
           ├─ AQI+气象全量数据下载
           ├─ 监测站坐标获取
           └─ Git仓库初始化 + 环境Dockerfile/requirements.txt

Week 2 ───┼─ 卫星影像下载（四季节选，100站×4季）
           ├─ OSM缓冲区特征提取
           ├─ 三模态时空对齐 → merged_dataset.h5
           └─ EDA报告（AQI时空分布、模态相关性热力图）

Week 3 ───┼─ ResNet-50 + EuroSAT 预训练权重加载+微调
           ├─ MM-CAF 完整网络实现 + 单元测试
           ├─ 训练循环（含Early Stop + Checkpoint + TensorBoard日志）
           └─ 首轮训练完成，baseline指标确认

Week 4 ───┼─ 超参数调优（Learning Rate / Dropout / 注意力头数）
           ├─ 消融实验（E0-E5全量运行）
           ├─ SHAP + 注意力权重可视化
           ├─ Streamlit应用框架 + 2个核心模块
           └─ 报告初稿完成

Week 5 ───┼─ 第17周 ─── 最终交付 ──────────────────── 95%成绩节点
           ├─ Streamlit剩余模块 + 一键运行脚本
           ├─ 报告终稿（10-20页，学术格式）
           ├─ 答辩PPT + 演示视频录制
           ├─ 代码清理 + README
           └─ 海报（A1，加分项）
```

---

## 五、技术可行性自检

### 5.1 硬件资源

| 资源 | 需求 | 实测参考 |
|------|------|---------|
| GPU显存 | 6GB | ResNet-50(batch=64, 224×224) + Attention(3×256) 约4.2GB |
| 磁盘 | 20GB | 图像400景≈1GB + EuroSAT≈2GB + 特征缓存≈5GB + 模型≈1GB |
| RAM | 16GB | 50K样本特征矩阵≈500MB，训练时DataLoader≈2GB |
| 网络 | 宽带 | Sentinel-2 400景≈800MB，首次下载约30分钟 |

### 5.2 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| Sentinel-2影像云覆盖率高 | 中 | 卫星模态弱化 | GEE月度中值合成；备选ESA WorldCover |
| 监测站精确坐标难以获取 | 低 | 空间对齐偏差 | akshare含城市名→OSM搜索匹配→人工校验 |
| Cross-Attention在小样本上过拟合 | 中 | 融合增益不明显 | 增大Dropout→0.3；若E5≈E4，则在报告中诚实分析 |
| OSM数据在某些城市稀疏 | 中 | OSM模态弱化 | 筛选OSM覆盖率>70%的城市；兰州/昆明等可能排除 |
| 时间不足 | 低 | 项目不完整 | Week 3若落后，降级至Early Fusion（无注意力），仍可答辩 |

---

## 六、附录：数据管线验证脚本

```python
"""
verify_data_pipeline.py
运行时间: ~15分钟（含首次EuroSAT下载）
目标: 验证全部数据源可达，无需完整下载即可确认项目数据管线可行
"""
import akshare as ak
import osmnx as ox
import openmeteo_requests
import requests_cache
from datasets import load_dataset
import pandas as pd

def verify_aqi():
    """验证AKShare AQI数据"""
    df = ak.air_quality_rank(date="2024-01-15")
    assert len(df) > 100, f"AQI数据量不足: {len(df)}"
    assert "pm2_5" in df.columns or "pm2.5" in df.columns, "PM2.5字段缺失"
    print(f"  [OK] AKShare: {len(df)}城市, 字段={list(df.columns)[:8]}...")
    return df

def verify_osm():
    """验证OSM数据（北京天坛附近）"""
    tags = {"amenity": True, "landuse": True, "highway": True}
    try:
        gdf = ox.features_from_point((39.8869, 116.4074), tags=tags, dist=1000)
        print(f"  [OK] OSM (osmnx): {len(gdf)}要素 在1km缓冲区内")
    except Exception as e:
        print(f"  [WARN] OSM查询超时(网络问题，非阻断): {e}")

def verify_weather():
    """验证Open-Meteo气象数据"""
    cache = requests_cache.CachedSession('.cache/metno', expire_after=3600)
    om = openmeteo_requests.Client(session=cache)
    params = {
        "latitude": 39.9, "longitude": 116.4,
        "start_date": "2024-01-01", "end_date": "2024-01-07",
        "daily": ["temperature_2m_mean", "relative_humidity_2m_mean",
                   "wind_speed_10m_mean", "surface_pressure_mean",
                   "precipitation_sum", "cloud_cover_mean"]
    }
    try:
        responses = om.weather_api("https://archive-api.open-meteo.com/v1/archive", params=params)
        daily = responses[0].Daily()
        print(f"  [OK] Open-Meteo: {len(daily.Variables(0).ValuesAsNumpy())}天气象数据")
    except Exception as e:
        print(f"  [WARN] Open-Meteo API异常: {e}")

def verify_eurosat():
    """验证EuroSAT数据集（HuggingFace）"""
    ds = load_dataset("blanchon/EuroSAT_RGB", split="train")
    print(f"  [OK] EuroSAT: {len(ds)}训练样本, {ds.features['label'].num_classes}类")
    print(f"       类别: {ds.features['label'].names}")

def main():
    print("=" * 60)
    print("城市环境质量多模态评估系统 —— 数据管线验证")
    print("=" * 60)

    print("\n[1/4] 验证 AQI 数据源 (AKShare)...")
    verify_aqi()

    print("\n[2/4] 验证 OSM 数据源 (osmnx)...")
    verify_osm()

    print("\n[3/4] 验证气象数据源 (Open-Meteo)...")
    verify_weather()

    print("\n[4/4] 验证卫星预训练基准 (EuroSAT)...")
    verify_eurosat()

    print("\n" + "=" * 60)
    print("全部数据源可达! 项目数据管线可行。")
    print("=" * 60)

if __name__ == "__main__":
    main()
```

---

> **计划书提交状态**：待第13周提交
> **下一阶段**：数据管线搭建（Week 1-2），详见第四节里程碑时间表
